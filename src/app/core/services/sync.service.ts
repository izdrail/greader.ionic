import { Injectable, signal } from '@angular/core'; import { Network } from '@capacitor/network'; import { StoragePort } from '../storage/storage.port'; import { ProviderAdapter } from '../providers/provider.port'; import { LocalProvider } from '../providers/local.provider'; import { FeedlyProvider,InoreaderProvider,OldReaderProvider } from '../providers/cloud.providers'; import { Account } from '../domain/models'; import { countNewArticles } from '../domain/app-settings';
@Injectable({providedIn:'root'}) export class SyncService { readonly running=signal(false); readonly lastError=signal<string|undefined>(undefined); constructor(private db:StoragePort,private local:LocalProvider,private feedly:FeedlyProvider,private inoreader:InoreaderProvider,private oldReader:OldReaderProvider){}
 async sync(account:Account):Promise<number>{if(!(await Network.getStatus()).connected) return 0; this.running.set(true);this.lastError.set(undefined);try{const p=this.provider(account);const pending=await this.db.pending(account.id);if(pending.length)await this.db.removePending(await p.push(account,pending));const s=await p.sync(account);const newArticles=countNewArticles(s.articles.map(a=>a.publishedAt),account.lastSyncAt);await Promise.all([this.db.putSubscriptions(s.subscriptions),this.db.putTags(s.tags),this.db.putArticles(s.articles)]);await this.db.putAccount({...account,lastSyncAt:Date.now()});return newArticles;}catch(e){this.lastError.set(e instanceof Error?e.message:String(e));throw e;}finally{this.running.set(false);}}
 private provider(a:Account):ProviderAdapter{return{local:this.local,feedly:this.feedly,inoreader:this.inoreader,'old-reader':this.oldReader}[a.provider];}}
