import { DirectoryService } from './directory.service';
describe('DirectoryService',()=>{
  it('restores feed links from APK feed/ identifiers',()=>{const service=new DirectoryService() as any;expect(service.feedUrl('feed/https://example.com/rss')).toBe('https://example.com/rss');expect(service.feedUrl('https://example.com/rss')).toBe('https://example.com/rss');});
  it('maps and deduplicates topic recommendations',async()=>{const response={ok:true,status:200,json:async()=>({feedInfos:[{feedId:'feed/https://example.com/rss',title:'Example',description:'Updates',iconUrl:'https://example.com/icon.png',subscribers:42},{id:'feed/https://example.com/rss',title:'Duplicate'}]})};vi.stubGlobal('fetch',vi.fn().mockResolvedValue(response));const rows=await new DirectoryService().recommendations(' angular ');expect(rows).toEqual([{title:'Example',url:'https://example.com/rss',description:'Updates',image:'https://example.com/icon.png',subscribers:42}]);expect(fetch).toHaveBeenCalledWith('/api/recommendations?topic=angular',{credentials:'same-origin'});vi.unstubAllGlobals();});
});
