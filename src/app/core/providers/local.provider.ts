import { Injectable } from '@angular/core'; import { FeedParserService } from '../feeds/feed-parser.service'; import { ProviderAdapter, SyncSnapshot } from './provider.port'; import { Account, PendingMutation } from '../domain/models';
@Injectable({providedIn:'root'}) export class LocalProvider extends ProviderAdapter { readonly kind='local' as const; constructor(private parser:FeedParserService){super();} async connect(){return{id:crypto.randomUUID(),provider:this.kind,label:'Local RSS',createdAt:Date.now()};} async sync():Promise<SyncSnapshot>{return{subscriptions:[],tags:[],articles:[]};} async push(_a:Account,c:PendingMutation[]){return c.map(x=>x.id);} parse(xml:string){return this.parser.parse(xml);} }
