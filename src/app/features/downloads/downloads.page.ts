import{Component,OnInit,signal}from'@angular/core';import{CommonModule}from'@angular/common';import{RouterLink}from'@angular/router';import{IonBackButton,IonButtons,IonContent,IonHeader,IonItem,IonLabel,IonList,IonTitle,IonToolbar}from'@ionic/angular';import{Article}from'../../core/domain/models';import{StoragePort}from'../../core/storage/storage.port';@Component({selector:'app-downloads',template:`<ion-header><ion-toolbar><ion-buttons slot="start"><ion-back-button defaultHref="/feeds"></ion-back-button></ion-buttons><ion-title>Offline downloads</ion-title></ion-toolbar></ion-header><ion-content><ion-list inset><ion-item *ngFor="let item of items()" [routerLink]="['/article',item.id]" button><ion-label><h2>{{item.title}}</h2><p>{{item.cached?'Available offline':'Not downloaded'}} · {{item.publishedAt|date:'mediumDate'}}</p></ion-label></ion-item></ion-list><p class="ion-padding" *ngIf="!items().length">No offline articles yet. Synced local feeds are cached automatically.</p></ion-content>`,imports:[CommonModule,RouterLink,IonHeader,IonToolbar,IonButtons,IonBackButton,IonTitle,IonContent,IonList,IonItem,IonLabel]})export class DownloadsPage implements OnInit{items=signal<Article[]>([]);constructor(private db:StoragePort){}async ngOnInit(){const a=(await this.db.listAccounts())[0];if(a)this.items.set((await this.db.listArticles(a.id)).filter(x=>x.cached));}}
