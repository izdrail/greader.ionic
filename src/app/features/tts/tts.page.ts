import{Component,OnInit,signal}from'@angular/core';import{CommonModule}from'@angular/common';import{IonBackButton,IonButton,IonButtons,IonContent,IonHeader,IonItem,IonLabel,IonList,IonTitle,IonToolbar}from'@ionic/angular';import{Article}from'../../core/domain/models';import{StoragePort}from'../../core/storage/storage.port';@Component({selector:'app-tts',template:`<ion-header><ion-toolbar><ion-buttons slot="start"><ion-back-button defaultHref="/feeds"></ion-back-button></ion-buttons><ion-title>Voice reading</ion-title><ion-buttons slot="end"><ion-button (click)="stop()">Stop</ion-button></ion-buttons></ion-toolbar></ion-header><ion-content><ion-list><ion-item *ngFor="let item of items();let i=index"><ion-label><h2>{{item.title}}</h2><p>{{item.author}}</p></ion-label><ion-button slot="end" (click)="play(i)">Play</ion-button></ion-item></ion-list></ion-content>`,imports:[CommonModule,IonHeader,IonToolbar,IonButtons,IonBackButton,IonTitle,IonButton,IonContent,IonList,IonItem,IonLabel]})export class TtsPage implements OnInit{items=signal<Article[]>([]);constructor(private db:StoragePort){}async ngOnInit(){const a=(await this.db.listAccounts())[0];if(a)this.items.set((await this.db.listArticles(a.id,{unreadOnly:true,limit:100})));}play(i:number){this.stop();const x=this.items()[i];speechSynthesis.speak(new SpeechSynthesisUtterance(`${x.title}. ${new DOMParser().parseFromString(x.content||'','text/html').body.textContent||''}`));}stop(){if('speechSynthesis'in window)speechSynthesis.cancel();}}
