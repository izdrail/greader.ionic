import {describe,it,expect,vi} from 'vitest';import worker from './src.js';
describe('worker feed endpoint',()=>{it('blocks loopback URLs before fetch',async()=>{vi.stubGlobal('fetch',vi.fn());const r=await worker.fetch(new Request('https://proxy.test/?url=http://127.0.0.1/rss'),{ALLOWED_ORIGIN:'https://reader.test'});expect(r.status).toBe(400);expect(fetch).not.toHaveBeenCalled();vi.unstubAllGlobals();});it('does not forward request authorization',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response('<rss/>',{headers:{'content-type':'application/rss+xml'}})));await worker.fetch(new Request('https://proxy.test/?url=https://example.com/rss',{headers:{Authorization:'Bearer secret'}}),{ALLOWED_ORIGIN:'https://reader.test'});expect(fetch.mock.calls[0][1].headers.Authorization).toBeUndefined();vi.unstubAllGlobals();});});
